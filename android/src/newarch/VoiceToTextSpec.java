package com.voicetotext;

import com.facebook.react.bridge.ReactApplicationContext;

abstract class VoiceToTextSpec extends NativeVoiceToTextSpec {
  VoiceToTextSpec(ReactApplicationContext context) {
    super(context);
  }
}
